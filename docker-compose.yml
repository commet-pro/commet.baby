services:
  postgres:
    image: postgres:16-alpine
    container_name: commet-baby-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: commet_baby
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d commet_baby"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  postgres_data:
